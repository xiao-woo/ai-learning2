# Cloudflare Workers Configuration
name = "ai-learning-sentences"
main = "src/worker.py"
compatibility_date = "2024-01-01"
compatibility_flags = ["python_workers"]

# 环境变量（在 Cloudflare Dashboard 设置）
[vars]
# API key 通过 Dashboard 配置

# 开发环境
[dev]
port = 8787
local_protocol = "http"
